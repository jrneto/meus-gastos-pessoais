# Tasks — FEAT-31: Confirmação de cadastro via código (OTP)

- [ ] 1. Em `features/auth/errors/authErrors.ts`: renomear `AccountPendingApprovalError` para `AccountNotConfirmedError`, com a mensagem "Confirme seu cadastro pelo código enviado por e-mail antes de entrar."; adicionar `InvalidConfirmationCodeError` com a mensagem "Código inválido ou expirado. Confira o email ou solicite um novo código."
- [ ] 2. Criar `features/auth/schemas/confirmationSchema.ts`: `confirmationCodeSchema = z.string().regex(/^\d{6}$/, 'Digite os 6 dígitos do código.')`
- [ ] 3. Criar `features/auth/schemas/confirmationSchema.test.ts`: aceita string de 6 dígitos, rejeita menos de 6, mais de 6, e string com caractere não numérico
- [ ] 4. Atualizar `features/auth/api/authApi.ts`: adicionar `ConfirmPayload`/`ResendConfirmationPayload` (interfaces conforme `plan.md`), `confirm(payload): Promise<void>` (`POST /auth/confirm`, 200 sem corpo, 400 → `InvalidConfirmationCodeError`, demais `!ok` → `UnknownAuthError`) e `resendConfirmation(payload): Promise<void>` (`POST /auth/resend-confirmation`, 200 sem corpo, `!ok` → `UnknownAuthError`); exportar as duas em `authApi`; atualizar a referência a `AccountPendingApprovalError` em `login()` para `AccountNotConfirmedError` (rename da task 1)
- [ ] 5. Atualizar `features/auth/api/authApi.test.ts`: renomear os casos existentes que usavam `AccountPendingApprovalError` para `AccountNotConfirmedError`; adicionar casos novos de `confirm` (200 sucesso, 400 `invalid-confirmation-code`, 400 `expired-confirmation-code`, erro de rede) e de `resendConfirmation` (200 sucesso, erro de rede)
- [ ] 6. Criar `features/auth/hooks/useConfirmAccount.ts`: mesmo formato de `useRegister.ts` (`confirm(payload)`, `isLoading`, `error`, `success`), sem tocar a `authStore`
- [ ] 7. Criar `features/auth/hooks/useConfirmAccount.test.ts`: sucesso liga `success`/desliga `isLoading`; erro (`InvalidConfirmationCodeError`) popula `error` e mantém `success=false`
- [ ] 8. Criar `features/auth/hooks/useResendConfirmation.ts`: mesmo formato de `useLogin.ts` mas sem `authStore` (`resend(payload)`, `isLoading`, `error`)
- [ ] 9. Criar `features/auth/hooks/useResendConfirmation.test.ts`: sucesso desliga `isLoading` sem erro; falha popula `error`
- [ ] 10. Criar `features/auth/hooks/useResendCooldown.ts`: `useResendCooldown(initialSeconds = 60)` retornando `{ secondsLeft, isExpired, restart }`, com `setInterval` decrescendo a cada 1s até 0 e `clearInterval` no unmount (`useEffect` cleanup) e ao chamar `restart()`
- [ ] 11. Criar `features/auth/hooks/useResendCooldown.test.ts` (com `vi.useFakeTimers()`): começa em `initialSeconds`; decresce 1 por segundo; `isExpired` vira `true` ao chegar a 0 e para de decrescer; `restart()` volta pra `initialSeconds` e recomeça a contagem; cleanup no unmount não deixa o interval rodando (sem warning de "state update on unmounted component")
- [ ] 12. Criar `features/auth/components/ConfirmationForm.tsx`: props `{ email, autoResendOnEnter, onConfirmed, onBack }` conforme `plan.md` — 6 inputs de 1 dígito (`aria-label="Dígito N do código"`) com avanço automático de foco e `Backspace` voltando ao campo anterior; submit valida com `confirmationCodeSchema` (bloqueia com "Digite os 6 dígitos do código." se inválido, sem chamar a API) e então chama `useConfirmAccount().confirm`; erro de confirmação exibido em `role="alert"` sem limpar os dígitos nem afetar o `useResendCooldown`; enquanto `secondsLeft > 0` mostra o contador (`M:SS`, rotulado como cooldown de reenvio) e o botão "Confirmar código" (spinner + "Confirmando…" durante `isLoading`); ao `isExpired`, desabilita os 6 inputs e troca o botão por "Reenviar e-mail", que chama `useResendConfirmation().resend`, limpa os dígitos, foca o primeiro input e chama `restart()`; se `autoResendOnEnter`, dispara `resend` uma vez no mount; "← Voltar" chama `onBack()` direto, sem chamar API
- [ ] 13. Criar `features/auth/components/ConfirmationForm.test.tsx`: preencher os 6 dígitos avança o foco automaticamente e digitar `Backspace` num campo vazio volta o foco; submit com menos de 6 dígitos é bloqueado no client (API não chamada); código correto chama `confirm` e dispara `onConfirmed(email)`; código incorreto (400) mostra o erro inline sem limpar os dígitos preenchidos; com `vi.useFakeTimers()`, avançar o relógio sem confirmar chega a `0:00`, desabilita os 6 inputs e mostra "Reenviar e-mail"; clicar em "Reenviar e-mail" chama `resendConfirmation`, limpa/reabilita os campos e reinicia o contador em 60s; `autoResendOnEnter=true` chama `resendConfirmation` no mount sem nenhuma interação; "← Voltar" chama `onBack` sem nenhuma chamada de API; erro de rede em `confirm` e em `resendConfirmation` mostra a mensagem de `NetworkError`
- [ ] 14. Reescrever `LoginForm.tsx`: trocar `authMode: 'login' | 'signup'` por `screen: 'login' | 'signup' | 'confirmation'` (mais `confirmationEmail`, `autoResendOnEnter`, `justConfirmedEmail`, conforme `plan.md`); o toggle "Entrar"/"Criar conta" só renderiza fora da tela `'confirmation'`; quando `screen === 'confirmation'`, renderiza só `<ConfirmationForm email={confirmationEmail} autoResendOnEnter={autoResendOnEnter} onConfirmed={...} onBack={...} />`
- [ ] 15. Atualizar `SignupForm` (mesmo arquivo): trocar prop `onDone: () => void` por `onRegistered: (email: string) => void`; remover a tela local de "Conta criada! Aguarde a aprovação..." — no `success` do `useRegister`, chama `onRegistered(data.email)` direto (via `useEffect` no `success` ou callback no `onSubmit`, guardando o email submetido)
- [ ] 16. Atualizar `LoginModeForm` (mesmo arquivo): adicionar props `justConfirmedEmail: string | null` (vira `defaultValues.email` do `useForm` e renderiza o banner "Email confirmado. Sua conta está ativa — entre com seus dados." quando não-nulo) e `onNeedsConfirmation: (email: string) => void`; quando o erro for `AccountNotConfirmedError`, exibir sua mensagem junto de um botão "Confirmar cadastro" que chama `onNeedsConfirmation(watch('email'))`
- [ ] 17. Atualizar `LoginForm.test.tsx`: ajustar o teste de cadastro com sucesso pra checar que a tela de confirmação aparece (6 inputs de dígito) no lugar da antiga mensagem de aprovação; mover/reescrever o teste "voltar da confirmação de cadastro" para `ConfirmationForm.test.tsx` (task 13); atualizar o teste de 401 `user-not-confirmed` pra checar a nova mensagem + botão "Confirmar cadastro"; adicionar `CONFIRM_URL`/`RESEND_URL` junto de `LOGIN_URL`/`REGISTER_URL`
- [ ] 18. Adicionar em `LoginForm.test.tsx` (ou teste de integração equivalente) o fluxo ponta a ponta: cadastro → tela de confirmação → código correto → volta ao login com o banner "Email confirmado..." e o campo email pré-preenchido
- [ ] 19. Adicionar em `LoginForm.test.tsx` o fluxo do CTA: login com 401 `user-not-confirmed` → clicar "Confirmar cadastro" → tela de confirmação abre com o email do login, dispara `resendConfirmation` automaticamente (sem clique adicional) e o contador de 60s em andamento
- [ ] 20. Rodar a suíte completa (`npm test`), `tsc -b`, `oxlint` e `npm run build`; garantir 100% dos testes passando
- [ ] 21. Revisão manual/visual: conferir a tela de confirmação contra `design-system/web/screenshots/21-otp-cadastro.png` e `22-otp-expirado.png`, e o login pós-confirmação contra `23-login-email-confirmado.png`; conferir responsividade contra o protótipo mobile equivalente
- [ ] 22. Atualizar `spec.md` marcando todos os critérios de aceite concluídos (`- [x]`)
