# Tasks — FEAT-22: Categorias — tipo e orçamento mensal

- [x] 1. Criar `lib/currency.ts` com `parseCurrencyToCents`, `formatCentsToCurrency` e `centsToAmountInput` (conteúdo movido de `features/expenses/utils/currency.ts`, sem mudança de implementação) e `lib/currency.test.ts` (conteúdo movido de `features/expenses/utils/currency.test.ts`)
- [x] 2. Atualizar `features/expenses/schemas/expenseSchema.ts`, `features/expenses/schemas/expenseFilterSchema.ts` e `features/expenses/components/ExpenseFormDialog.tsx` para importar de `@/lib/currency`; deletar `features/expenses/utils/currency.ts` e `features/expenses/utils/currency.test.ts`; rodar a suíte de `expenses` e confirmar 100% passando (garante que a movida não quebrou nada antes de seguir) — também atualizados `ExpenseDetailDialog.tsx`/`ExpenseList.tsx` (importavam `formatCentsToCurrency`, não listados no plan)
- [x] 3. Atualizar `lib/categories/types.ts`: `CategoryItem` perde `cor`/`icone`, ganha `tipo: 'despesa' | 'receita'` e `orcamentoMensalCents: number | null`
- [x] 4. Deletar `lib/categories/categoryIcons.ts` (sem substituto — nenhuma tela usa seletor de ícone)
- [x] 5. Reescrever `features/categories/schemas/categorySchema.ts`: `nome`, `tipo` (`enum` despesa/receita), `orcamentoMensal` (string opcional, regex `0,00`, só relevante para despesa) → `.transform` pra `orcamentoMensalCents` (`undefined` quando tipo≠despesa ou campo vazio) → `.refine` rejeitando `orcamentoMensalCents` ≤ 0 (mensagem no campo `orcamentoMensal`)
- [x] 6. Criar `features/categories/schemas/categorySchema.test.ts`: nome vazio/longo, tipo ausente, teto com formato inválido, teto igual a zero/negativo, teto omitido em categoria de despesa (válido, `orcamentoMensalCents` undefined), teto preenchido mas tipo=receita (`orcamentoMensalCents` undefined mesmo assim), e o caso 100% válido (despesa com teto, despesa sem teto, receita)
- [x] 7. Atualizar `features/categories/api/categoriesWriteApi.ts`: `CategoryPayload` troca `cor`/`icone` por `tipo`/`orcamentoMensalCents?`; atualizar `features/categories/api/categoriesWriteApi.test.ts` (payloads dos testes existentes + casos de despesa com/sem teto e receita) — também atualizados os fixtures de `CategoryDeleteDialog.test.tsx`, `useRegisterCategory.test.ts` e `useUpdateCategory.test.ts` (usavam `cor`/`icone`, não listados no plan; só pegos por `tsc -b`, o runtime do Vitest não valida tipos)
- [x] 8. Adicionar tokens `--color-positive`, `--color-positive-100`, `--color-positive-700` em `styles/modernist/modernist.css`, escopados em `.ds-modernist` (valores `oklch(...)` copiados do `.dc.html`)
- [x] 9. Atualizar `lib/categories/CategoryLetterTile.tsx`: nova prop opcional `tipo?: 'despesa' | 'receita'` aplicando cor de borda/fundo (`--color-accent*` para despesa, `--color-positive*` para receita); sem a prop, mantém o estilo neutro atual inalterado
- [x] 10. Atualizar `lib/categories/CategoryLetterTile.test.tsx`: cobrir os três casos (`tipo` omitido → neutro, `tipo="despesa"` → cores accent, `tipo="receita"` → cores positive) — asserções via `element.style.border` em vez de `toHaveStyle` (jsdom não resolve `var(...)` em `getComputedStyle`)
- [x] 11. Reescrever `features/categories/components/CategoryForm.tsx`: seletor de tipo (`.seg`/`.seg-opt`, radio `despesa`/`receita`) sempre visível; campo "Teto mensal (R$)" visível só quando tipo = despesa; trocar de Despesa pra Receita chama `resetField('orcamentoMensal')`; `defaultValues`/`initialValues` sem `cor`/`icone`
- [x] 12. Atualizar `features/categories/components/CategoryForm.test.tsx`: criar despesa sem teto, criar despesa com teto, criar receita (sem campo de teto no DOM), trocar tipo despesa→receita esconde e descarta o teto preenchido, teto inválido (zero/negativo/formato) bloqueia submit com mensagem no campo, editar categoria trocando o tipo, editar categoria alterando/removendo o teto (mantendo os testes já existentes de erro de nome duplicado/not-found adaptados ao novo payload)
- [x] 13. Reescrever `features/categories/components/CategoryList.tsx`: dividir `items` em `expenseItems`/`incomeItems` (filtro por `tipo`); renderizar duas seções ("Categorias de despesa", "Categorias de receita"); item de despesa mostra teto formatado (`formatCentsToCurrency`) ou "Sem teto definido"; item de receita não mostra valor; `initialValues` passados ao `CategoryForm` de edição incluem `tipo` e `orcamentoMensal` (via `centsToAmountInput`) no lugar de `cor`/`icone`; `CategoryLetterTile` recebe a prop `tipo` do item
- [x] 14. Atualizar `features/categories/components/CategoryList.test.tsx`: lista vazia por seção, categorias de despesa e receita aparecendo nas seções corretas, despesa com/sem teto exibindo o texto certo, receita sem nenhum valor monetário, edição abrindo o form com `tipo`/`orcamentoMensal` pré-preenchidos corretamente
- [x] 15. Atualizar `routes/CategoriesPage.tsx`: título "Categorias e orçamentos"; atualizar `routes/CategoriesPage.test.tsx` de acordo
- [x] 16. Rodar a suíte completa (`npm test`), `tsc -b`, `oxlint` e `npm run build`; garantir 100% dos testes passando — 312/312 testes, `tsc -b` limpo, `oxlint` sem warnings novos, build de produção ok
- [x] 17. Revisão manual/visual: conferir a tela de Categorias (criar despesa com/sem teto, criar receita, editar trocando tipo, editar teto, excluir dos dois tipos) contra `frontend/design-system/web/jrnexpenses-web.dc.html` (tela "Categorias e orçamentos") — validado ao vivo (API local + cognito-local, usuário de teste registrado/confirmado, categorias seedadas via API, app real no navegador via Playwright): lista com as duas seções, teto formatado/"Sem teto definido", seletor de tipo alternando e escondendo o campo de teto, formulário de edição pré-preenchido corretamente
- [x] 18. Atualizar `spec.md` marcando todos os critérios de aceite concluídos (`- [x]`)
