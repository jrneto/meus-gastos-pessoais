# Tasks — FEAT-21: Cadastro real

- [ ] 1. Criar `features/auth/utils/cpf.ts`: `extractDigits(value, maxLen)`, `maskCpf(digits)` (`000.000.000-00` progressivo), `isValidCpf(digits)` (algoritmo de dígito verificador do CPF + rejeita sequência de dígitos repetidos)
- [ ] 2. Criar `features/auth/utils/cpf.test.ts`: `extractDigits` remove não-dígitos e respeita `maxLen`; `maskCpf` formata progressivamente (1, 4, 7, 11 dígitos); `isValidCpf` aceita CPF válido conhecido, rejeita dígito verificador errado e rejeita todas as sequências repetidas (`00000000000`...`99999999999`)
- [ ] 3. Criar `features/auth/utils/phoneMask.ts`: `maskPhone(digits)` (`(11) 98765-4321` para 11 dígitos, `(11) 8765-4321` para 10, progressivo)
- [ ] 4. Criar `features/auth/utils/phoneMask.test.ts`: formata progressivamente com 10 e com 11 dígitos
- [ ] 5. Criar `features/auth/schemas/registerSchema.ts` substituindo `signupSchema.ts`: campos `name` (trim, 2-150), `phoneDigits` (regex `^\d{10,11}$`), `cpfDigits` (regex `^\d{11}$` + `.refine(isValidCpf, ...)`), `email`, `password` (min 8) — mensagens de erro em português, mesmo estilo de `loginSchema.ts`
- [ ] 6. Criar `features/auth/schemas/registerSchema.test.ts` cobrindo cada regra de validação (nome vazio/curto/longo, telefone com 9 ou 12 dígitos, CPF com dígito verificador inválido, CPF com sequência repetida, email inválido, senha curta) e o caso 100% válido
- [ ] 7. Deletar `features/auth/schemas/signupSchema.ts` e `signupSchema.test.ts`
- [ ] 8. Adicionar em `features/auth/errors/authErrors.ts`: `EmailAlreadyExistsError`, `CpfAlreadyExistsError`, `RegisterValidationError`, `AccountPendingApprovalError` (mensagens conforme `plan.md`)
- [ ] 9. Atualizar `features/auth/api/authApi.ts`: adicionar `register(payload: RegisterPayload): Promise<RegisterResponse>` (interfaces `RegisterPayload`/`RegisterResponse` conforme `plan.md`), tratando 409 `email-already-exists`/`cpf-already-exists` (via `problem.type`), 400 (`RegisterValidationError`) e demais erros (`UnknownAuthError`); exportar `register` em `authApi`
- [ ] 10. Atualizar `authApi.login` para inspecionar `problem.type` num 401: `user-not-confirmed` → `AccountPendingApprovalError`; qualquer outro → `InvalidCredentialsError` (mantém o retorno de sucesso inalterado)
- [ ] 11. Atualizar `features/auth/api/authApi.test.ts`: casos novos de `register` (sucesso 201, 409 email, 409 cpf, 400, erro de rede) e casos novos de `login` (401 `user-not-confirmed` → `AccountPendingApprovalError`, 401 `invalid-credentials` → `InvalidCredentialsError`, mantendo os testes existentes passando)
- [ ] 12. Criar `features/auth/hooks/useRegister.ts`: mesmo formato de `useLogin.ts` (`register(payload)`, `isLoading`, `error`), mas sem tocar a `authStore` (não há sessão a guardar) — expõe também um estado `success: boolean` para o formulário saber quando mostrar a confirmação
- [ ] 13. Criar `features/auth/hooks/useRegister.test.ts`: sucesso liga `success`/desliga `isLoading`; erro popula `error`/mantém `success=false`
- [ ] 14. Atualizar `features/auth/hooks/useLogin.test.ts` (se necessário) para cobrir o novo mapeamento de erro do `authApi.login` chegando intacto até o hook (sem lógica nova no hook, só garantindo que `error` recebe a instância certa)
- [ ] 15. Reescrever `SignupForm` em `features/auth/components/LoginForm.tsx`: campos nome, CPF (com `maskCpf`/`extractDigits`), telefone (com `maskPhone`/`extractDigits`), email, senha — CPF e telefone em grid de 2 colunas (`display:'grid', gridTemplateColumns:'1fr 1fr', gap:...`); usa `registerSchema`/`useRegister`; botão com label em gerúndio ("Criando conta...") durante `isLoading`; ao `success`, exibe mensagem inline de confirmação ("Conta criada! Aguarde a aprovação do administrador para poder entrar.") e volta o `LoginForm` pro modo "Entrar" (`authMode='login'`)
- [ ] 16. Atualizar `features/auth/components/LoginForm.test.tsx`: cadastro com sucesso mostra a confirmação e volta pro modo login; 409 email/cpf mostram as mensagens específicas; 400 mostra mensagem genérica; erro de rede mostra `NetworkError` e mantém os campos preenchidos; máscaras aplicadas nos campos de CPF/telefone durante a digitação
- [ ] 17. Atualizar `LoginModeForm` (mesmo arquivo) para exibir a mensagem de `AccountPendingApprovalError` quando for esse o erro do `useLogin`, mantendo a mensagem existente para os demais casos
- [ ] 18. Adicionar teste em `LoginForm.test.tsx` (ou onde já existem os testes de login) cobrindo `AccountPendingApprovalError` exibindo a mensagem específica, e `InvalidCredentialsError` continuando com a mensagem já existente
- [ ] 19. Remover a rota `/cadastro-em-breve` e o import de `SignupComingSoonPage` em `app/router.tsx`
- [ ] 20. Deletar `routes/SignupComingSoonPage.tsx` e `routes/SignupComingSoonPage.test.tsx`
- [ ] 21. Rodar a suíte completa (`npm test`), `tsc -b`, `oxlint` e `npm run build`; garantir 100% dos testes passando
- [ ] 22. Revisão manual/visual: conferir que o modo "Criar conta" da tela de login corresponde ao design (`design-system/web/screenshots/02-criar-conta.png`, `03-criar-conta-preenchida.png`), que o modo "Entrar" não mudou de aparência, e que nenhuma navegação para rota autenticada acontece após o cadastro
- [ ] 23. Atualizar `spec.md` marcando todos os critérios de aceite concluídos (`- [x]`)
