# Tasks — FEAT-32: Recuperação de senha (fluxo completo)

- [ ] 1. Em `features/auth/errors/authErrors.ts`: adicionar `InvalidResetCodeError` com a mensagem "Código inválido ou expirado." e `WeakPasswordError` com a mensagem "A senha deve ter no mínimo 8 caracteres, com letra maiúscula, minúscula, número e símbolo."
- [ ] 2. Criar `features/auth/schemas/passwordPolicySchema.ts`: `passwordPolicySchema` com `.min(8, ...)` + `.regex(/[A-Z]/, ...)` + `.regex(/[a-z]/, ...)` + `.regex(/[0-9]/, ...)` + `.regex(/[^A-Za-z0-9]/, ...)`, cada um com a mensagem específica conforme `plan.md`
- [ ] 3. Criar `features/auth/schemas/passwordPolicySchema.test.ts`: senha válida passa; senha sem maiúscula, sem minúscula, sem número, sem símbolo e com menos de 8 caracteres falham, cada uma com a mensagem correspondente
- [ ] 4. Atualizar `features/auth/schemas/registerSchema.ts`: trocar `password: z.string().min(8, 'A senha deve ter no mínimo 8 caracteres.')` por `password: passwordPolicySchema`
- [ ] 5. Atualizar `features/auth/schemas/registerSchema.test.ts`: remover/ajustar o caso antigo de "mínimo 8 caracteres" e adicionar casos cobrindo a política completa (senha de 8+ caracteres sem maiúscula/minúscula/número/símbolo cada uma rejeitada; senha que atende tudo aceita)
- [ ] 6. Criar `features/auth/schemas/forgotPasswordSchema.ts`: `forgotPasswordEmailSchema` (`{ email: z.string().email('Informe um email válido.') }`) e `newPasswordSchema` (`{ newPassword: passwordPolicySchema, confirmNewPassword: z.string() }` com `.refine` comparando os dois campos, mensagem "As senhas não coincidem." em `confirmNewPassword`)
- [ ] 7. Criar `features/auth/schemas/forgotPasswordSchema.test.ts`: `forgotPasswordEmailSchema` aceita email válido e rejeita inválido/vazio; `newPasswordSchema` aceita quando os dois campos coincidem e atendem a política, rejeita quando divergem (mesmo com ambos válidos individualmente) e propaga o erro de `passwordPolicySchema` quando `newPassword` é fraco
- [ ] 8. Atualizar `features/auth/api/authApi.ts`: adicionar `ForgotPasswordPayload`/`ResetPasswordPayload` (interfaces conforme `plan.md`), `forgotPassword(payload): Promise<void>` (`POST /auth/forgot-password`, 200 sem corpo, `!ok` → `UnknownAuthError`) e `resetPassword(payload): Promise<void>` (`POST /auth/reset-password`, 200 sem corpo, 400 com `type` terminando em `bad-request` → `WeakPasswordError`, qualquer outro 400 → `InvalidResetCodeError`, demais `!ok` → `UnknownAuthError`); exportar as duas em `authApi`
- [ ] 9. Atualizar `features/auth/api/authApi.test.ts`: adicionar casos de `forgotPassword` (200 sucesso, erro de rede) e de `resetPassword` (200 sucesso, 400 `bad-request` → `WeakPasswordError`, 400 `invalid-reset-code` → `InvalidResetCodeError`, 400 `expired-reset-code` → `InvalidResetCodeError`, erro de rede)
- [ ] 10. Criar `features/auth/hooks/useForgotPassword.ts`: mesmo formato de `useResendConfirmation.ts` (`forgotPassword(payload)`, `isLoading`, `error`), sem tocar a `authStore`
- [ ] 11. Criar `features/auth/hooks/useForgotPassword.test.ts`: sucesso desliga `isLoading` sem erro; falha popula `error`
- [ ] 12. Criar `features/auth/hooks/useResetPassword.ts`: mesmo formato de `useConfirmAccount.ts` (`resetPassword(payload)`, `isLoading`, `error`, `success`), sem tocar a `authStore`
- [ ] 13. Criar `features/auth/hooks/useResetPassword.test.ts`: sucesso liga `success`/desliga `isLoading`; erro (`InvalidResetCodeError` e `WeakPasswordError`, separadamente) popula `error` e mantém `success=false`
- [ ] 14. Criar `features/auth/components/OtpDigitsInput.tsx`: extrair de `ConfirmationForm.tsx` o grid de 6 `<input>` de 1 dígito com `aria-label` por dígito, avanço automático de foco ao digitar e `Backspace` voltando ao campo anterior — props `{ digits, disabled, onChange }` conforme `plan.md`, sem nenhuma lógica de submit/cooldown/API
- [ ] 15. Refatorar `features/auth/components/ConfirmationForm.tsx` para renderizar `<OtpDigitsInput />` no lugar do grid inline, mantendo `inputRefs`/foco e todo o restante do comportamento idêntico; rodar `ConfirmationForm.test.tsx` (já existente) e confirmar que continua passando sem nenhuma alteração no arquivo de teste
- [ ] 16. Criar `features/auth/components/ForgotPasswordFlow.tsx` com o esqueleto do state machine (`step: 'email' | 'code' | 'new-password'`, `email`, `code`) e o Passo 1/3 (`ForgotPasswordEmailStep`, função interna): `useForm` com `forgotPasswordEmailSchema`; submit chama `useForgotPassword().forgotPassword({ email })` e, em sucesso, avança pro Passo 2/3 com o email submetido; "← Voltar ao login" chama `onBack()` (prop de `ForgotPasswordFlow`) sem chamar API
- [ ] 17. Adicionar `ForgotPasswordCodeStep` (função interna de `ForgotPasswordFlow.tsx`, Passo 2/3): reaproveita `<OtpDigitsInput />` e `useResendCooldown(60)`; "Confirmar código" só valida `confirmationCodeSchema` (bloqueia com "Digite os 6 dígitos do código." se inválido) e chama `onConfirmed(code)` **sem chamar nenhuma API**; contador chegando a zero desabilita os campos e troca o botão por "Reenviar e-mail", que chama `useForgotPassword().forgotPassword({ email })` de novo, limpa os campos e reinicia o cooldown; "← Voltar" chama `onBack()` (volta ao Passo 1/3, dentro do state machine de `ForgotPasswordFlow`)
- [ ] 18. Adicionar `NewPasswordStep` (função interna de `ForgotPasswordFlow.tsx`, Passo 3/3): `useForm` com `newPasswordSchema`; submit chama `useResetPassword().resetPassword({ email, code, newPassword })`; sucesso dispara `onSuccess()`; erro `WeakPasswordError`/`NetworkError` exibido inline, permanecendo no passo; erro `InvalidResetCodeError` exibido inline com um botão "Voltar e conferir o código" que chama `onBackToCode()`
- [ ] 19. Ligar os 3 passos em `ForgotPasswordFlow` (função principal): `step === 'email'` → `ForgotPasswordEmailStep`; `step === 'code'` → `ForgotPasswordCodeStep`; `step === 'new-password'` → `NewPasswordStep`; transições conforme `plan.md` (`onSent` guarda o email e avança; `onConfirmed` guarda o código e avança; `onBackToCode` volta ao Passo 2/3; sucesso do Passo 3/3 chama `onDone(email)` da prop de `ForgotPasswordFlow`)
- [ ] 20. Criar `features/auth/components/ForgotPasswordFlow.test.tsx`: Passo 1/3 com email de conta existente avança pro Passo 2/3; Passo 1/3 com email inexistente avança igual, sem nenhuma diferença observável (US2); Passo 2/3 com 6 dígitos avança pro Passo 3/3 sem nenhuma chamada de API; Passo 2/3 com código incompleto bloqueia o submit no client; contador do Passo 2/3 chegando a zero (via `vi.useFakeTimers()`) desabilita os campos e troca pelo botão de reenvio, que chama `forgot-password` de novo e reinicia o contador; "← Voltar" do Passo 2/3 volta ao Passo 1/3 com o email preservado, sem chamar API; Passo 3/3 com senhas diferentes bloqueia o submit no client; Passo 3/3 com sucesso chama `onDone(email)`; Passo 3/3 com erro de código (400 `invalid-reset-code`) mostra o botão "Voltar e conferir o código" e, ao clicar, volta ao Passo 2/3; Passo 3/3 com senha fora da política (400 `bad-request`) mantém o usuário no passo, mostrando o erro; erro de rede em cada uma das 3 chamadas (`forgot-password` nos Passos 1/3 e 2/3, `reset-password` no Passo 3/3)
- [ ] 21. Atualizar `features/auth/components/LoginForm.tsx`: adicionar `'forgot-password'` ao union `Screen`; quando `screen === 'forgot-password'`, renderizar só `<ForgotPasswordFlow onDone={...} onBack={() => setScreen('login')} />`, ocupando o card inteiro (sem o seletor "Entrar"/"Criar conta")
- [ ] 22. Refatorar `features/auth/components/LoginForm.tsx`: trocar `justConfirmedEmail: string | null` por `banner: LoginBanner` (`{ email: string; message: string } | null`, conforme `plan.md`); `ConfirmationForm.onConfirmed` passa a chamar `setBanner({ email, message: 'Email confirmado. Sua conta está ativa — entre com seus dados.' })`; `ForgotPasswordFlow.onDone` chama `setBanner({ email, message: 'Senha redefinida. Entre com a nova senha.' })` e volta `screen` pra `'login'`
- [ ] 23. Atualizar `LoginModeForm` (mesmo arquivo): trocar a prop `justConfirmedEmail` por `banner: LoginBanner`, usando `banner?.email ?? ''` como `defaultValues.email` e `banner?.message` no corpo do aviso existente (mesmo bloco visual, sem duplicar JSX); adicionar o link "Esqueci minha senha" abaixo do campo "Senha", chamando a nova prop `onForgotPassword: () => void` (→ `setScreen('forgot-password')` em `LoginForm`, sem chamar API)
- [ ] 24. Atualizar `LoginForm.test.tsx`: ajustar o teste do banner pós-confirmação de cadastro pra continuar passando com `banner` no lugar de `justConfirmedEmail`; adicionar `FORGOT_PASSWORD_URL`/`RESET_PASSWORD_URL` junto de `CONFIRM_URL`/`RESEND_URL`/`LOGIN_URL`/`REGISTER_URL`; adicionar teste do link "Esqueci minha senha" abrindo o Passo 1/3 do fluxo
- [ ] 25. Adicionar em `LoginForm.test.tsx` o fluxo ponta a ponta: link "Esqueci minha senha" → Passo 1/3 (email) → Passo 2/3 (código) → Passo 3/3 (nova senha) → sucesso volta ao login com o banner "Senha redefinida..." e o campo email pré-preenchido
- [ ] 26. Rodar a suíte completa (`npm test`), `tsc -b`, `oxlint` e `npm run build`; garantir 100% dos testes passando
- [ ] 27. Revisão manual/visual: conferir os 3 passos contra `design-system/web/screenshots/24-recuperar-senha.png`, `25-otp-recuperacao.png` e `26-nova-senha.png`, e o login pós-redefinição contra `27-login-senha-redefinida.png`; conferir responsividade contra os protótipos mobile equivalentes
- [ ] 28. Atualizar `spec.md` marcando todos os critérios de aceite concluídos (`- [x]`)
