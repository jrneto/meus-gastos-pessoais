# URL base da API de homologação do backend (backend/specs/FEAT-13-ambiente-homologacao/).
# Usado pelo build de homologação (`npm run build:hom`). Ver
# frontend/docs/constitution.md e frontend/specs/FEAT-01-setup-login/plan.md
# para o racional de nunca versionar .env* reais (só este .env.hom.example).
VITE_API_BASE_URL=https://api-hom.jrnexpenses.com
